Python command to automate the creation of new project locally.

* The command is used to create in /var/www new folder;
* will create new nginx config file;
* will update /etc/hosts file with alias for new project.
